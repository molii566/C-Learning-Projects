#include <stdio.h>

int main() {
    // A for loop that counts down from 10 to 1
    for (int i = 10; i >= 1; i--) {
        printf("%d \n", i);  // Print the current value of 'i'
    }

    return 0;  // Exit the program
}
